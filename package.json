{
  "name": "hub-crawl",
  "version": "2.0.0",
  "description": "Curriculum crawler checks for broken links in the App Academy curriculum.",
  "main": "index.js",
  "scripts": {
    "start": "mkdir -p dist && babel src --out-dir dist --extensions .js && node --harmony ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.22.2",
    "babel-loader": "^6.2.10",
    "babel-preset-es2015": "^6.22.0",
    "nightmare": "^2.9.1",
    "readline-sync": "^1.4.6",
    "webpack": "^2.2.1"
  },
  "devDependencies": {
    "eslint": "^3.16.1",
    "eslint-config-airbnb-base": "^11.1.0",
    "eslint-plugin-import": "^2.2.0"
  }
}
